<h1 class="mb-6 text-xl text-center font-extrabold mt-6 md:mt-16  leading-none tracking-tight text-gray-900 md:text-4xl">
Endroit populaire à visiter</h1>






<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mx-6 md:mx-16">
 {% for listing in listings %}
 
    {% set chemin = listing.images | length > 0 ? "template/uploads/" ~ listing.images[0].chemin : "template/images/no.jpg" %}
    <div class="relative h-[300px]" >
        <img class=" h-full w-full object-cover group-hover:scale-105 transition-transform duration-500 ease-in-out" src="{{ asset(chemin)}}" alt="">
         <div class="absolute bottom-0 left-0 right-0 bg-black/30 backdrop-blur-md p-4">
        <p class="text-white text-center text-xl">{{ listing.name | raw | upper}}</p>
    </div>
    </div>
    {% endfor %}
  
</div>

